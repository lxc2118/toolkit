#if(${useTddl} == false)
jdbc.driverClassName=org.gjt.mm.mysql.Driver
jdbc.maxActive=20
jdbc.maxIdle=30
jdbc.maxWait=3000
jdbc.url=${url}
jdbc.username=${username}
jdbc.password=${password}
#else
appname=${appname}
dbGroupKey=${dbGroupKey}
#end